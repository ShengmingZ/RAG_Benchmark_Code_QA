

# top-1
def retrieval_eval(doc_key):
    """
    follow DPR: if answer is in retrieval docs, then retrieval right, and set a random text as oracle
    :return:
    """


def pred_eval(pred):
    """
    follow DPR and In-context RALM, if pred matches to any of the answers, then exact match
    :param pred:
    :return:
    """